#!groovy
//noinspection GroovyUnusedAssignment
@Library("Infrastructure") _

import uk.gov.hmcts.contino.Environment

Environment environment = new Environment(env)

onMaster {
  sharedInfrastructurePipeline('ia', environment.nonProdName, 'nonprod')
  sharedInfrastructurePipeline('ia', environment.demoName, 'nonprod')
  sharedInfrastructurePipeline('ia', environment.prodName, 'prod')
}
